# Go (labs/go)
labs/go/bin/
*.exe
*.test
*.out

# Spin (labs/spin) — el verificador y sus trazas se regeneran desde el .pml
labs/spin/pan
labs/spin/pan.*
labs/spin/*.trail
labs/spin/*.nvr
labs/spin/_spin_nvr.tmp

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# OS
.DS_Store

# Datos del TB (cofinanciamiento) — el CSV de AidData son 1,23 GB, se baja aparte
labs/go/cofinanciamiento/data/
aiddata*.csv
